{
  "name": "file-upload-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-client": "yarn --cwd packages/client start",
    "start-api": "yarn --cwd packages/api start",
    "start": "concurrently \"yarn start-client\" \"yarn start-api\"",
    "test": "yarn --cwd packages/client test --no-cache"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Malcolm Laing",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.1"
  }
}
